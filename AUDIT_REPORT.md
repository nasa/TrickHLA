# Audit Report — TrickHLA

**Auditor:** Astrolexis.space — Kulvex Code
**Date:** 2026-04-12
**Project:** /home/curly/projects/TrickHLA
**Languages:** cpp, python, shell, c, java

---

## Summary

- Files scanned: **500**
- Candidates found: **107**
- Confirmed findings: **2**
- False positives: **97**
- Scan duration: 1786.1s

### Severity breakdown

| Severity | Count |
|----------|-------|
| 🟡 MEDIUM | 2 |

---

## Findings

### 1. 🟡 Bare except: catches SystemExit and KeyboardInterrupt — CWE-396

**File:** `sims/IMSim/SIM_IMSim_Test/RUN_a_side/input.py:19`
**Severity:** MEDIUM
**Pattern:** `py-013-bare-except`

**Why this matters:**
A bare except: clause catches ALL exceptions including SystemExit (sys.exit()), KeyboardInterrupt (Ctrl+C), and GeneratorExit. This can prevent clean shutdown and make the program unkillable.

**Code:**
```cpp
17:                       + '\', setting the variable server source address to 127.0.0.1!' )
18:                trick.var_server_set_source_address( '127.0.0.1' )
19:          except:
20:             return  # Use host source address as is.
21:    except ( socket.error, socket.gaierror, socket.herror, socket.timeout ):
22:       print( 'WARNING: Problem resolving \'' + trick.var_server_get_hostname()
```

**Verification:** Line 19 contains a bare `except:` clause with no specified exception type, which catches all exceptions including `KeyboardInterrupt` and `SystemExit`.

**Execution path:** A `KeyboardInterrupt` (Ctrl+C) or `SystemExit` occurring during the execution of `subprocess.check_output` or the subsequent logic (lines 13-18) will be caught by the bare `except:` on line 19.

**Suggested fix:**
```
Change `except:` to `except Exception:` or a more specific exception like `except (subprocess.CalledProcessError, OSError):`.
```

---

### 2. 🟡 Bare except: catches SystemExit and KeyboardInterrupt — CWE-396

**File:** `sims/JEOD/SIM_Master/RUN_test/input.py:120`
**Severity:** MEDIUM
**Pattern:** `py-013-bare-except`

**Why this matters:**
A bare except: clause catches ALL exceptions including SystemExit (sys.exit()), KeyboardInterrupt (Ctrl+C), and GeneratorExit. This can prevent clean shutdown and make the program unkillable.

**Code:**
```cpp
118:                       + '\', setting the variable server source address to 127.0.0.1!' )
119:                trick.var_server_set_source_address( '127.0.0.1' )
120:          except:
121:             return  # Use host source address as is.
122:    except ( socket.error, socket.gaierror, socket.herror, socket.timeout ):
123:       print( 'WARNING: Problem resolving \'' + trick.var_server_get_hostname()
```

**Verification:** Line 120 contains a bare `except:` clause which catches all exceptions, including `KeyboardInterrupt` and `SystemExit`, potentially preventing the program from being terminated.

**Execution path:** A `KeyboardInterrupt` (Ctrl+C) or `SystemExit` occurring during the execution of the inner try block (lines 114-119) will be caught by the bare except at line 120.

**Suggested fix:**
```
Change `except:` to `except Exception:` on line 120.
```

---

## Methodology

This audit was produced by the KCode audit engine: a deterministic pattern library scanned the project for known-dangerous code patterns, then every candidate was verified against the actual execution path. Findings listed here are only those where the execution path was confirmed.

**Pattern library version:** 1.0 — patterns derived from real bugs found in production C/C++ codebases (network I/O, USB/HID decoders, resource lifecycle, integer arithmetic).

---

*Generated by KCode — [Astrolexis.space](https://astrolexis.dev)*
